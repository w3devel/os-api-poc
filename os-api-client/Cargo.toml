[package]
name = "os-api-client"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Client library for the local OS-API broker proof-of-concept"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
