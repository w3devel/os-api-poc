[package]
name = "os-api-example"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Example app using the local OS-API broker client"

[dependencies]
os-api-client = { path = "../os-api-client" }
